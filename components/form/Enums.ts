/**
 * SECTION: UI enums
 */
export enum SelectedCount {
  SINGLE = "single",
  MULTIPLE = "multiple",
}

export enum ColumnCount {
  TWO = "group-2",
  THREE = "group-3",
}

/**
 * SECTION: data enums
 */

export enum EntityType {
  INDIVIDUAL = "1",
  COMPANY = "2",
}
export enum IdType {
  IC_NO = "1",
  CO_REG_NO = "2",
}
